# Frontend — College Event Management

Tech: Vite + React + TypeScript + Tailwind + React Query

Run dev: `npm ci && npm run dev`

Notes
- UI library placeholder: `shadcn/ui` components can be added on top of this scaffold.
- Add protected routes and `RoleGuard` components to show role-specific dashboards.
- Use React Query for server cache and background sync.
